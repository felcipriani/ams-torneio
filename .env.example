# Session Token Salt
# Generate a secure random salt for session token hashing
# Example: openssl rand -hex 32
# REQUIRED for production
SESSION_TOKEN_SALT=your-secure-random-salt-here

# Optional: Trusted proxy IPs (comma-separated)
# Use this if your application is behind a load balancer or proxy
# TRUSTED_PROXY_IPS=127.0.0.1,10.0.0.0/8
